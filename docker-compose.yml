version: '3.8'

services:
  whatsapp-webhook:
    build: .
    container_name: whatsapp_webhook_server
    ports:
      - "80:8000"
    environment:
      - VERIFY_TOKEN=my_secure_verify_token_123
    restart: always
