# This Caddyfile automatically secures your domain with HTTPS (Let's Encrypt)
# and redirects all HTTP traffic to HTTPS. Let's Encrypt requires a valid domain name.

# If you have a real domain name pointing to the EC2, use it below (e.g., api.yourdomain.com).
# If you don't have one, we use a free nip.io domain based on your instance's public IP.
# If your IP changes, you will need to update this file!
{$DOMAIN:13.250.57.121.nip.io} {
    reverse_proxy whatsapp-webhook:8000
}
